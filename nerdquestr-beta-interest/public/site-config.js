/*
 * ONLY EDIT THESE TWO SETTINGS.
 * Never paste a password, SMTP credential, API secret, or main-site database key here.
 * The Formspree /f/... endpoint is a public form address, NOT a secret API key.
 */
window.NERDQUESTR_BETA_CONFIG = Object.freeze({
  // Paste the endpoint from your OWN Formspree form between the quotation marks.
  formspreeEndpoint: "",

  // Set true only AFTER verifying beta@nerdquestr.com, the recipient in Formspree,
  // and reviewing/approving public/privacy.html and the launch checklist in README.md.
  launchApproved: false
});
